# Optional: for LOCAL preview only. GitHub Pages ignores this and builds the
# site server-side with its own pinned `github-pages` gem set (no Actions).
#
# To preview locally (once, if you ever install bundler):
#   gem install --user-install bundler
#   bundle install
#   bundle exec jekyll serve --livereload
#
source "https://rubygems.org"
gem "github-pages", group: :jekyll_plugins
gem "webrick" # needed for local serve on Ruby 3.x
