# Auto-generated by .github/workflows/update-publications.yml (bi-weekly) from
# NASA ADS. Leave the values null until the ADS_TOKEN repo secret is added and
# the workflow runs once; _includes/stats.html falls back to _data/metrics.yml.
# Do not hand-edit: the sync fully overwrites this file.
updated: ""
refereed:
citations:
h_index:
reads:
