# Auto-generated by .github/workflows/ads-metrics.yml (weekly) from NASA ADS.
# Leave the values null until the ADS_TOKEN repo secret is added and the
# workflow runs once; _includes/stats.html falls back to _data/metrics.yml.
updated: ""
refereed:
citations:
h_index:
reads:
